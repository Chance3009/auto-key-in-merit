name: Deploy to GCP VM

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Debug SSH Key
        run: |
          echo "Checking SSH key format..."
          echo "${{ secrets.SSH_KEY }}" > deploy_key
          chmod 600 deploy_key
          echo "Key file permissions:"
          ls -la deploy_key
          echo "Key file format check:"
          ssh-keygen -l -f deploy_key || echo "Invalid key format"
          echo "SSH version:"
          ssh -V
          echo "Testing verbose SSH connection:"
          ssh -vvv -i deploy_key -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} 'echo "SSH Test"' || echo "SSH test failed"
          rm deploy_key

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 22
          debug: true
          command_timeout: 30m
          use_insecure_cipher: false
          cipher: aes256-ctr
          script: |
            echo "=== Deployment Started: $(date) ==="
            echo "SSH User: $USER"
            echo "Current directory: $PWD"
            echo "Testing git access..."
            cd ~/auto-key-in-merit || echo "Failed to cd to directory"
            git status || echo "Failed to get git status"
            echo "Pulling latest changes..."
            git pull origin main || echo "Failed to pull from git"
            echo "Activating virtual environment..."
            source venv/bin/activate || echo "Failed to activate venv"
            echo "Installing requirements..."
            pip install -r requirements.txt || echo "Failed to install requirements"
            echo "Restarting service..."
            sudo /bin/systemctl restart merit || echo "Failed to restart service"
            echo "=== Deployment Completed: $(date) ==="
            # Log SSH access
            echo "Last SSH access: $(date)" >> ~/ssh_access.log
            # Check SSH key in authorized_keys
            echo "Checking authorized_keys:"
            cat ~/.ssh/authorized_keys
